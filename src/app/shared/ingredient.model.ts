export class Ingredient {
    constructor (public name: string, public amount: number) {}
}
//public name: string;
//public amount: number;
         // this.name = name;
         // this.amount = amount;
// The constructor with PUBLIC inside does everything that is labeled in green as a shortcut